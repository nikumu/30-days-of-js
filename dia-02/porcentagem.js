const porcentagem = (numerador, denominador) => {
    var resultado = numerador / denominador
    console.log(numerador + " equivale a " + resultado * 100 + "% de " + denominador)
}

porcentagem(50, 100)
